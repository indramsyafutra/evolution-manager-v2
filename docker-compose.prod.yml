services:
  evolution-manager:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:80"
    container_name: evolution-manager-v2
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    networks:
      - evolution-network

networks:
  evolution-network:
    driver: bridge

